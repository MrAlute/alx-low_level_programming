#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two integers
 * @a and b:  int type letter
 * @b:  int type letter
 * Return: 0
 */

int mul(int a, int b)
{
	c = a * b;

	printf("%d", c);
	return (0);
}
